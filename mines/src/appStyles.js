import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container: {
      flex: 1,
      justifyContent: 'flex-end',
    },
    welcome: {
      fontSize: 20,
      textAlign: 'center'
    },
    board: {
        alignItems: 'center',
        backgroundColor: '#AAA',
    }
  });

  export { styles }